version '1.0-SNAPSHOT'

apply plugin: 'application'
apply plugin: 'docker'
apply plugin: 'kotlin'

mainClassName = 'hervalicious.unforgiven.BotRunner'

dependencies {
    compile project(":commons")
}